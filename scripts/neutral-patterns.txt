# Patterns that must never appear in the tracked tree.
#
# This file feeds scripts/check-neutral.sh, which CI runs on every push.
# Purpose: the repository must stay free of any one operator's private
# values (registry namespace, cloud identifiers, resource names, Telegram
# chat ids) — this guard stops them slipping in via a copy-pasted command
# or a "temporary" test value.
#
# One extended regex per line; lines starting with # are comments.
# Each pattern wraps one character in [brackets] so this file matches the
# forbidden literal without containing it — `git grep <identifier>` over
# the tracked tree must return nothing, including here.
# LICENSE is exempted by the script (a copyright notice names the rights
# holder on purpose).

# Maintainer's GitHub / registry namespace and name.
yesh[d]mits
[Yy]eshe[n]ko
Dmyt[r]o
julius[b]aer

# Private deployment's brand prefix (resource names, advisory lock).
va[r]ve

# Azure subscription and deploy-SP object id (GUID prefixes).
e1d1e[4]9c
1ed7a[5]7a

# Real Telegram chat ids (ops chat and the three channels).
54121[2]8446
44050[8]4898
43320[6]0514
43929[5]2953
